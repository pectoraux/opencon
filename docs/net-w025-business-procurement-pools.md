# NET-W025 — Business procurement pools

**Status:** IMPLEMENTED on `feat/net-w025-business-procurement-pools` — full local gate green; PR submitted for architect review
**GitHub issue:** #50
**Work order:** `spec/work-orders/NET-W025.md`
**Architecture:** v1.0 FROZEN
**Dependencies:** NET-W024 + NET-W008 merged

## Purpose

This document is the durable evidence ledger for NET-W025. It allows an architect or reviewer with no conversation history to verify — from the repository alone — that business procurement pools are implemented INSIDE the frozen `/demand` boundary (no second demand/procurement authority) on the W024 privacy foundations, with explicit buyer-organization/actor authorization, competition-policy-governed deterministic aggregation behind the frozen commitment floor AND the frozen distinct-organization floor, minimized supplier-facing disclosure (bands/buckets/windows only — never exact quantities, unit prices, budgets or timing), tenant isolation, idempotency/concurrency/atomicity conventions, and zero economic-authority surface.

## Architectural decision record

NET-W025 introduces NO second authority. The authority split of record:

- **`/demand` owns** the procurement POOL records (tenant-scoped, versioned procurement category + dual-threshold qualification/competition policy, one-way closure), the private business COMMITMENT records (buyer-organization references + bounded provider-neutral attributes + server-written `aggregate_disclosure` consent grants + one-way withdrawal), the closed versioned procurement vocabularies (`src/core/procurement.ts`), the pure privacy/competition-policy aggregation engine, and the derived supplier-facing minimized demand contract. `/demand` remains the single demand/aggregate semantic authority established by NET-W024 — W025 EXTENDS the boundary, it does not duplicate it.
- **`/settlement` stays the sole economic authority**: there is NO economic mutation surface in `/demand` (no ledger, credits, cash, stakes, rewards, value records). Business commitments mint nothing; supplier offers/selection are NET-W026 and savings are NET-W027.
- **`/identity`, `/organizations`, `/participants` stay the membership/authorization authorities**: commitment authorization resolves server-side through the NEUTRAL membership lookup (the same thin read-only adapter over the /organizations membership repository, wired at the bootstrap root): the acting person must hold ACTIVE membership in BOTH the tenant organization AND the named buyer organization. Client claims never fabricate buyer eligibility, membership or qualification.
- **`/workflows` is untouched**: procurement pools and commitments carry NO lifecycle subject kind; closure and withdrawal are ONE-WAY field mutations (the W019/W024 retirement precedent).
- **No coupling** to `/evidence`, `/outcomes`, `/reputation`, `/disputes`, `/campaigns`, `/inventory`, `/adapters`: qualification cannot be influenced by activity, spend, wealth or reputation — no such input exists.

## Implementation shape (the decision of record, as shipped)

1. **Neutral vocabulary** — `src/core/procurement.ts` (core tier, NEW): `PROCUREMENT_CATEGORY_KEYS` (8 provider-neutral business verticals, `PROCUREMENT_CATEGORY_VERSION "1"`); region codes REUSED from the W024 demand contracts (`ProcurementRegionCode = DemandRegionCode` — the same 12 neutral macro-regions); `PROCUREMENT_BUDGET_BANDS` (5 business-scale currency-neutral bands); `PROCUREMENT_UNIT_PRICE_BANDS` (5 currency-neutral bands — prices cross ONLY as bands); `PROCUREMENT_TIMING_WINDOWS` (5 coarse windows); quantity bounds 1..1000000 + `PROCUREMENT_QUANTITY_BUCKETS` (5 business-scale buckets) + `procurementQuantityBucket`; **`PROCUREMENT_PRIVACY_MINIMUM_COMMITMENTS = 3` AND `PROCUREMENT_PRIVACY_MINIMUM_ORGANIZATIONS = 3` (the TWO frozen disclosure floors — no policy or caller can lower either; the organization floor is the PROC-003 competition dimension)**; `PROCUREMENT_CONSENT_SCOPE "aggregate_disclosure"` + `PROCUREMENT_CONSENT_VERSION "NET-W025:1"`; dual-threshold policy bounds; prose bounds; record formats `NET-W025:1`; `InvalidProcurementError` (PROCUREMENT_VALIDATION/validation) + `ProcurementCommitmentConflictError` (PROCUREMENT_COMMITMENT_CONFLICT/conflict); pure validators. Data + pure validation only.
2. **Port** — `src/demand/port.ts` (domain tier, EXTENDED additively): records (`ProcurementPool`, `ProcurementCommitment` with `buyerOrganizationId` + `submittedBy`, `ProcurementConsentGrant`), inputs/results, the derived view types (`QualifiedProcurementAggregate`, `ProcurementAggregateFacts`, `ProcurementAggregateCheck` with 6 checks, `ProcurementDistributionGroup`), repository interfaces with `...WithinTx` twins, `ProcurementService` + deps, the `DemandPort` audit vocabulary extended with the four procurement event types. Contracts only (no function bodies — the port pin).
3. **Authority repositories** — `src/demand/authority-procurement-repositories.ts` (NEW): PostgresAuthority-backed append-only collections `procurement_pools` / `procurement_commitments`; one-way `closeWithinTx` / `withdrawWithinTx`; `findActiveByPoolAndSubmitterWithinTx` (the create-once constraint — per (pool, submitter), NOT per buyer organization, so the commitment count and the distinct-organization count stay independent dimensions and the competition floor stays meaningful); `listActiveByPool` (the derivation input); deterministic (createdAt, id) ordering.
4. **The pure aggregation engine** — `src/demand/procurement-aggregation-engine.ts` (NEW): `deriveQualifiedProcurementAggregate` (no I/O, no wall clock — the anchor arrives as an argument) emits the six re-derived checks (`pool_open`, `requestor_membership`, `commitments_present`, `privacy_floor_met`, `organization_floor_met`, `qualification_thresholds_met`), the minimized aggregate facts (commitmentCount + organizationCount — the only organization datum that ever crosses, itself floor-gated — + five bounded distributions + `suppressedGroups`), group suppression at the frozen commitment floor (below-floor groups counted, NEVER named), aggregate emission gated on (commitment floor met AND organization floor met AND requestor an active member), the deterministic SHA-256 digest over the canonical sorted-key decision facts (anchor EXCLUDED — identical state ⇒ identical digest; reuses the /demand `demandDigest` helper), and `hasValidProcurementConsent`. Counts in check details are disclosed only under the same gate as the aggregate facts.
5. **The domain service** — `src/demand/procurement-pool-service.ts` (NEW): material commands (pool create/close, commitment create/withdraw) follow the full convention set — validation → server-resolved acting person → TENANT ANCHOR FIRST (cross-tenant = NotFoundError, no existence oracle) → the DUAL membership gates (tenant membership for every command; buyer-organization membership for commitments — a failed buyer authorization is indistinguishable from a nonexistent organization) → owner gates (creator-only closure, submitter-only withdrawal) → composite idempotency keys (`procurement_pool:{org}:{key}`, `procurement_pool_close:{org}:{poolId}:{key}`, `procurement_commitment:{org}:{poolId}:{submitter}:{key}`, `procurement_commitment_withdraw:{org}:{commitmentId}:{key}`) → per-pool advisory lock `procurement_pool_commitment:{org}:{poolId}` (conservation under concurrency) → `applyIdempotent` on ONE authoritative transaction → in-tx fresh reads + gate re-derivation (TOCTOU closure) → `...WithinTx` writes → transactional audit buffer (`procurement_pool.created|closed`, `procurement_commitment.recorded|withdrawn`) → COMMIT. The derived `evaluateQualifiedProcurementDemand` mutates and audits NOTHING (a derived 200 decision).
6. **Bootstrap** — `src/bootstrap/runtime.ts` (MODIFIED, additive): procurement repositories + `createProcurementService` reusing the SAME neutral membership lookup (over `membershipRepo.findByPersonAndOrganization` — it answers for ANY organization: the tenant gate AND the buyer-organization gate alike); `Runtime.procurementService`; eight apiCommands methods with view shapers.
7. **API** — `src/api/port.ts` + `src/api/server.ts` (MODIFIED, additive): guard actions `demand.procurement.pools.create|close`, `demand.procurement.commitments.create|withdraw|read`, `demand.procurement.aggregates.evaluate`; routes POST /api/demand/procurement/pools (201), POST /api/demand/procurement/pools/:id/closure (200), POST /api/demand/procurement/pools/:id/qualified-aggregate (200 derived decision), POST /api/demand/procurement/commitments (201), POST /api/demand/procurement/commitments/:id/withdrawal (200), POST /api/demand/procurement/commitments/mine (200 — the ONLY commitment read surface, actor-scoped server-side), GET /api/demand/procurement/pools/:id + GET /api/demand/procurement/pools (public tenant-scoped pool metadata only). Status semantics 201/200/400/403/404/409.
8. **Tests** — `tests/demand/` (W025 harness wrapping the W024 harness: three buyer organizations, dual memberships, seeded guard actions + 7 AC suites, 49 tests) + `tests/regression/net-w025-ac-08-architecture-out-of-scope.test.ts` (10 tests) + the `ac-08-no-premature-domain-logic` amendment (`NET_W025_DOMAINS` + the non-skeletal module assertion — the established per-work-item pattern).

## Evidence matrix

| AC | Required evidence | Status |
|---|---|---|
| AC-01 first-class records with provenance + buyer-organization authorization + consent | `tests/demand/net-w025-ac-01-procurement-records.test.ts` — 8 tests: durable pool + commitment records, provenance/lineage fields, buyer-organization reference + server-written consent grant, closed vocabularies (category/region/quantity/budget/price/timing/consent) fail closed, name/prose/policy bounds, tenant-scoped listings with filters | GREEN |
| AC-02 deterministic qualification (dual thresholds) | `tests/demand/net-w025-ac-02-deterministic-qualification.test.ts` — 7 tests: identical-state digest reproducibility (anchor excluded), PURE-ENGINE order independence, governing-fact digest sensitivity, withdrawn exclusion, closed-pool semantics (qualified false; aggregate still disclosable — the W024-consistent contract), commitment-threshold boundary, organization-threshold boundary + complete check set + nothing-derived-stored | GREEN |
| AC-03 privacy/competition-preserving aggregation | `tests/demand/net-w025-ac-03-privacy-competition.test.ts` — 9 tests: EXACT minimized fact contract, no identifier-shaped strings beyond pool/org references (no buyer-org/person/commitment ids), exact quantities never cross (bucket-only; below-floor buckets suppressed), bands/windows only (exact terms unrepresentable), THE ORGANIZATION FLOOR (many-commitment single-buyer aggregate suppresses entirely — PROC-003), THE COMMITMENT FLOOR, below-floor group suppression (counted, never named), withdrawal immediacy, zero mutation/audit side effects + error contexts carry no private attribute material | GREEN |
| AC-04 explicit, unassertable thresholds and floors | `tests/demand/net-w025-ac-04-threshold-policy.test.ts` — 5 tests: versioned dual-threshold policy on the record + view, frozen floors pinned as constants (3/3), NEITHER floor lowerable by pool policy (loosest 1/1 policy still suppresses below the floors), caller-asserted aggregates/counts/qualification ignored (smuggled fields), no activity/spend/wealth/reputation input exists (source scan) | GREEN |
| AC-05 idempotency, concurrency, conservation | `tests/demand/net-w025-ac-05-idempotency-concurrency.test.ts` — 7 tests: same-key replay exactly-once (one record, one audit event), 4-way concurrent same-key exactly-one, 4-way concurrent distinct submitters conserved (4 commitments / 3 organizations), withdrawal exactly-once, one-active-per-(pool, submitter) conflict + same-organization-multi-member allowed, withdraw-recommit new record, pool create + close replay | GREEN |
| AC-06 tenancy + dual-membership authorization fail closed | `tests/demand/net-w025-ac-06-tenancy-authorization.test.ts` — 8 tests: cross-tenant not-found with existence-oracle parity, cross-tenant create/evaluate fail closed, non-tenant-member mutations denied, buyer-organization authorization fails closed indistinguishable from nonexistent organizations, non-member suppressed views (200 decision, counts suppressed), submitter-only withdraw + creator-only close, full HTTP status semantics (403/201/400/200/404/200/409/403), deny-by-default on the bare runtime | GREEN |
| AC-07 atomicity + audit lineage | `tests/demand/net-w025-ac-07-atomicity-audit.test.ts` — 5 tests: atomic commit with transactionId/idempotencyRecordId/execution lineage, FAILURE INJECTION (no record + no audit on rollback, replayable key), withdrawal/closure audit events with buyer-org metadata, creation event metadata (dual-threshold policy), closed-world audit vocabulary (evaluations audit nothing) | GREEN |
| AC-08 architecture / out-of-scope | `tests/regression/net-w025-ac-08-architecture-out-of-scope.test.ts` — 10 tests: both guards 0 violations (≥290 files), frozen specs (no 17th domain; no /procurement boundary; /demand stays the single home), work-order binding (v1.0 FROZEN, DEM-001..003 + PROC-001..003, #50), vocabulary pins (all new + every prior frozen vocabulary including W024's demand set), NO economic/lifecycle/reputation machinery + no cross-domain imports + no W026/W027/W028 vocabulary, privacy containment (actor-scoped-only commitment surface; /demand owns the vocabulary exclusively), economic + lifecycle authorities untouched, composition-root wiring pins, complete file list, no secrets + no new configuration | GREEN |

## Mutation evidence

Six targeted mutations (cp-backup + assert-applied + assert-failed-with-expected-regression + restore + assert-green; driver script in the session workspace only, never committed to the repository):

1. **Organization-floor removal** (aggregates emitted regardless of distinct organizations) — CAUGHT by AC-03/AC-04 (the single-buyer suppression test + the loose-policy test failed).
2. **Commitment-floor removal** — CAUGHT by AC-03 (below-floor suppression test failed).
3. **Group-suppression removal** (below-floor groups named) — CAUGHT by AC-03 (suppressed-group test failed).
4. **Buyer-organization membership gate removal** (any actor may commit for any organization) — CAUGHT by AC-06 (buyer-authorization tests failed).
5. **Tenant-scope removal** (cross-tenant pool readable) — CAUGHT by AC-06 (cross-tenant not-found tests failed).
6. **Idempotency randomization** (composite key randomized) — CAUGHT by AC-05 (same-key replay test failed).

All six restored green after each check (the driver verified the suite passed again after every restore).

## Privacy / competition evidence

- Individual business commitments are PRIVATE records: the ONLY commitment read surface is the actor-scoped `POST /api/demand/procurement/commitments/mine` (the submitter is the server-resolved authenticated actor — there is no submittedBy input anywhere) plus the owner's own mutation results. No other route returns individual commitment data.
- The supplier-facing view (`QualifiedProcurementAggregate`) carries ONLY: pool identity/scope, category + policy (public), the six machine-readable checks, the minimized aggregate facts (commitmentCount + organizationCount + five bounded distributions + suppressed-group count), the deterministic digest, and the single evaluation anchor. AC-03 proves structurally: no UUID-shaped strings beyond the explicit pool/org references, no buyer-organization/person/commitment identifiers, no exact per-organization quantities (bucket distributions only), no exact unit prices, budgets or timing (closed band/window vocabularies only — exact terms are unrepresentable in the type system), exactly the eight-key aggregate contract.
- The TWO frozen floors (`PROCUREMENT_PRIVACY_MINIMUM_COMMITMENTS = 3` AND `PROCUREMENT_PRIVACY_MINIMUM_ORGANIZATIONS = 3`) suppress ALL aggregate facts — including both counts in check details — below either floor, and suppress every below-floor distribution group (counted in `suppressedGroups`, never named). The distinct-organization floor is the PROC-003 competition semantic: a many-commitment single-buyer aggregate suppresses ENTIRELY (AC-03), so no supplier or co-member can attribute any fact to one organization.
- The create-once constraint is per (pool, submitter) — buyer organizations MAY hold multiple commitments from different authorized members; the commitment count and the organization count stay independent dimensions and the organization floor governs the competition dimension.
- Withdrawal is the consent revocation and takes effect on the very next evaluation (no retention). Evaluations mutate nothing and audit nothing.
- Error contexts on the procurement surface carry only field/id/scope lineage — never private attribute or consent payloads (AC-03).
- No secrets, no configuration, no key material exist in this work item (deterministic derivation only); the config schema and `.env.example` are untouched.

## Final verification record

- `bun run verify` (typecheck + `arch:check` + `authority:check` + full suite): **1625 pass / 15 skip / 0 fail / 18000 expect() / 1640 tests / 206 files** (post-W024 baseline: 1565 pass / 1580 tests — +60).
- `arch:check` + `authority:check`: **290 files / 0 violations** (baseline 286 — the four new source files).
- Mutation checks: **6/6 CAUGHT**, restored green after each.
- Integration: the procurement material path runs on the SAME PostgreSQL authority + IdempotencyStore + transactional-audit machinery exercised by the configured real PostgreSQL/Redis integration job (CI `integration`); the domain introduces no new provider.
- W025 acceptance tests: 59 (49 demand + 10 regression) + 1 amended skeleton-activation test = 60 new passing tests.
- Staged-diff secret scan: clean (no credential patterns; no new configuration entries).
- PR: (recorded below after submission).
- Review state: awaiting architect review — **NOT merged** (merge only on green CI AND recorded architect approval).
- Merge SHA: PENDING.

## Completion rule

Merge only after implementation, acceptance coverage, mutation checks, full verification, green CI, and recorded architect approval. After merge, update `spec/PROJECT-STATE.md` and the roadmap pointers with the merge SHA before advancing to NET-W026.
